/**
 * FieldBadge.jsx
 * AI & plagiarism detection has been removed for now — this renders nothing.
 * (Kept as a no-op so existing imports/usages don't need changing.)
 */
export default function FieldBadge() {
  return null;
}
